# from Andy Skelton: http://andy.wordpress.com/2010/06/14/my-ssh-config-setup/
# Do not kill connection if route is down temporarily.
TCPKeepAlive no

# Allow ten minutes down time before giving up the connection.
ServerAliveCountMax 30
ServerAliveInterval 20

# Conserve bandwith. (Compression is off by default.)
Compression yes

# Use ssh keys on remote hosts
# Useful for git cloning on a remote server
ForwardAgent yes
